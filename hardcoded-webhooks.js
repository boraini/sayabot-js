/* If you want to use specific channel webhooks for any guild channel, add them here as objects.
 * 
 * Each key-value pair should look like this:
 * channelId: { id, token, channelId }
 * 
 * token is the Webhook token. The rest are string Snowflakes.
 */

export const hardcodedWebhooks = {
};